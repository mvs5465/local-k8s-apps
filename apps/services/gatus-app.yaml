apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: gatus
  namespace: argocd
  finalizers:
  - resources-finalizer.argocd.argoproj.io
spec:
  project: cluster
  source:
    repoURL: https://twin.github.io/helm-charts
    targetRevision: 1.5.0
    chart: gatus
    helm:
      releaseName: gatus
      values: |
        replicaCount: 1

        image:
          repository: twinproduction/gatus
          tag: latest

        resources:
          requests:
            cpu: 50m
            memory: 128Mi
          limits:
            cpu: 200m
            memory: 256Mi

        ingress:
          enabled: true
          ingressClassName: nginx
          path: /
          pathType: Prefix
          hosts:
            - gatus.local

        persistence:
          enabled: true
          size: 500Mi
          mountPath: /data

        config:
          storage:
            type: sqlite
            path: /data/gatus.db

          ui:
            title: Service Status

          endpoints:
            - name: prometheus
              url: http://prometheus-server.monitoring.svc.cluster.local
              interval: 60s
              conditions:
                - "[STATUS] == 200"

            - name: grafana
              url: http://grafana.monitoring.svc.cluster.local/api/health
              interval: 60s
              conditions:
                - "[STATUS] == 200"

            - name: loki
              url: http://loki.monitoring.svc.cluster.local:3100/ready
              interval: 60s
              conditions:
                - "[STATUS] == 200"

            - name: homepage
              url: http://homepage.services.svc.cluster.local:3000
              interval: 60s
              conditions:
                - "[STATUS] == 200"

            - name: argocd
              url: http://argocd-server.argocd.svc.cluster.local/healthz
              interval: 60s
              conditions:
                - "[STATUS] == 200"

            - name: gatus
              url: http://gatus.services.svc.cluster.local/health
              interval: 60s
              conditions:
                - "[STATUS] == 200"

            - name: jellyfin
              url: http://jellyfin.services.svc.cluster.local:8096/health
              interval: 60s
              conditions:
                - "[STATUS] == 200"

  destination:
    server: https://kubernetes.default.svc
    namespace: services

  syncPolicy:
    syncOptions:
    - CreateNamespace=true
    automated:
      prune: true
      selfHeal: true
