# Example Outline Secrets Configuration
#
# Copy this file to ~/.secrets/outline/ and populate with your values
# Each value should be in a separate file in that directory
#
# For example:
#   cp outline-secrets.example ~/.secrets/outline/secret_key
#   echo "your-secret-value" >> ~/.secrets/outline/secret_key
#
# Or generate random values:
#   openssl rand -hex 32 > ~/.secrets/outline/secret_key
#   openssl rand -hex 32 > ~/.secrets/outline/utils_secret
#

# Secret key for Outline (used for encryption/authentication)
# Generate with: openssl rand -hex 32
secret_key=your-generated-secret-key-here

# Utils secret (used for internal utilities)
# Generate with: openssl rand -hex 32
utils_secret=your-generated-utils-secret-here

# PostgreSQL database credentials
postgres_user=outline
postgres_password=your-secure-database-password
postgres_db=outline
